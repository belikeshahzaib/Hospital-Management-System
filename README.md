# Hospital Management System (DSA Project)

A comprehensive C++ console application designed to manage hospital operations efficiently using advanced Data Structures.

## 🚀 Features
- **Doctor Management:** Add, update, and delete doctor profiles (Linked List).
- **Patient Records:** Quick lookup and storage of patient data (Hash Map).
- **Appointment System:** FIFO processing of patient appointments (Queue).
- **Data Persistence:** Automatically saves and loads data from `.txt` files.

## 🛠️ Data Structures Used
- **Linked List:** Used for managing the dynamic list of doctors.
- **Queue:** Used to handle the appointment line (First-In, First-Out).
- **Unordered Map (Hash Table):** Used for O(1) average time complexity when searching for patients by ID.
- **File I/O:** For permanent storage of records.

## 💻 How to Run
1. Clone the repo: `git clone https://github.com/your-username/repo-name.git`
2. Compile: `g++ main.cpp -o hospital_system`
3. Run: `./hospital_system`
